# Diagnostics And Illegal Cases

PTO manual pages should distinguish:

- type-class errors
- legality failures
- target-profile restrictions
- unsupported behavior that should not be documented as legal

Every instruction set and per-op page should name the cases that are not allowed instead of leaving them implicit.
