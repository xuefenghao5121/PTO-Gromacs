# pto.tsethf32mode

旧路径兼容入口。规范页现已移动到标量/控制路径下的 [pto.tsethf32mode](../../../scalar/ops/control-and-configuration/tsethf32mode_zh.md)。

## 为什么移动

`pto.tsethf32mode` 虽然保留历史 `t` 前缀，但其架构角色是控制 / 配置状态，而不是 tile payload 变换。
